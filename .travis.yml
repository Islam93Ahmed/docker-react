sudo: required

services:
    - docker

before_install:
    - docker build -t islam93ahmed/docker-react -f Dockerfile.dev .
    
script:
    - docker run islam93ahmed/docker-react set CI=true && npm test